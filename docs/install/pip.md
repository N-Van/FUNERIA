# Installation avec Pip

L'installation avec pip se déroulera dans de bonnes conditions si cette
conditon est satisfaite :

- Python 3.10 doit être installé

```sh
# python 3.10
python -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
```
